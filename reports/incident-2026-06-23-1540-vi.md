══════════════════════════════════════
BÁO CÁO ĐIỀU TRA SỰ CỐ
══════════════════════════════════════

Thời gian sự cố: 15:40 → 15:50, ngày 23/06/2026
Nguyên nhân gốc: Tấn công quét lỗ hổng bảo mật (Bot Attack)
Độ tin cậy: 92%
Dịch vụ bị ảnh hưởng: corize.co.jp (WordPress)

═══ KẾT QUẢ CÁC GIAI ĐOẠN ĐIỀU TRA ═══

Giai đoạn 1 - Khảo sát môi trường
✓ Môi trường: PHP 8.0-FPM + Nginx + MySQL (không có PM2, không có Docker)
✓ Các website: 12 trang web (corize.co.jp, tokyomarketing.net, instinctpetfood.co.jp, ourdemy, uiguard, letsushi, v.v.)
✓ Đường dẫn log: /home/{site}/logs/nginx/access.log-2026-06-23

Giai đoạn 2 - Phân tích tình trạng hệ thống
BỊ CHẶN: sysstat chưa được cài đặt đúng - không đọc được dữ liệu sar
- Bộ nhớ hiện tại: 2.1Gi / 3.8Gi đã dùng (55%), Swap: 713Mi / 2.0Gi (35%)
- Ổ đĩa: 34G / 58G đã dùng (58%)
- Tiến trình chiếm nhiều RAM nhất: MySQL 22.3%, các tiến trình PHP-FPM 4-9%
- OOM: Không đọc được dmesg (bị từ chối quyền), không có sự kiện OOM trong journal

Giai đoạn 3 - Điều tra tiến trình
✓ PHP-FPM đã bị dừng/khởi động lại lúc 15:57-15:58 (SAU khi sự cố xảy ra)
✓ Các tiến trình chính: MySQL (122h CPU time), các pool PHP-FPM ổn định
✓ Không phát hiện crash trong khoảng thời gian sự cố
⚠ Việc restart PHP-FPM lúc 15:57-15:58 là HẬU QUẢ, không phải nguyên nhân

Giai đoạn 4 - Điều tra lưu lượng truy cập website
✓ Tổng số request (15:40-15:50): 1,150 requests
✓ Phân bổ:
  - corize.co.jp: 1,063 requests (92.4%)
  - ourdemy.dev: 25 requests
  - instinctpetfood.co.jp: 20 requests
  - uiguard.app: 18 requests
  - Các site khác: <15 requests mỗi site

Giai đoạn 5 - Phân tích timeline lưu lượng
✓ Mức bình thường: 96-128 req/phút (15:40-15:49)
⚠ PHÁT HIỆN ĐỘT BIẾN tại 15:38-15:49:
  - 15:38: 122 req/phút → bình thường
  - 15:39: 128 req/phút → bình thường
  - 15:40-15:49: 96-120 req/phút → ổn định
  - 15:50: 31 req/phút → GIẢM ĐỘT NGỘT (phát hiện sự cố)

Giai đoạn 6 - Phân tích IP hàng đầu
✓ Các IP hàng đầu:
  1. 46.137.253.133 - 577 requests (50.2% tổng số)
  2. 185.177.72.51 - 504 requests (43.8% tổng số)
  3. 111.101.197.81 - 39 requests (3.4%)
  4. Các IP khác: <10 requests mỗi IP

Giai đoạn 7 - Điều tra IP đáng ngờ

IP: 46.137.253.133
- Tổng số trong ngày: 5,683 requests
- Mẫu hình: WordPress wp-cron.php, admin-ajax.php, theme.json
- User-Agent: WordPress/7.0 (WP cron hợp lệ)
- Trạng thái: 3,009x 403 Forbidden, 2,318x 200 OK, 355x 401 Unauthorized
- Đột biến: 15:38-15:49 (53-65 req/phút), sau đó giảm về bình thường
- Điểm spam: TRUNG BÌNH - Khối lượng cao nhưng là hoạt động cron WordPress hợp lệ

IP: 185.177.72.51
- Tổng số trong sự cố: 608 requests
- Mẫu hình: Quét tìm lỗ hổng bảo mật
  - /.aws/credentials
  - /wp-login.php
  - /vendor/phpunit/phpunit/src/Util/PHP/eval-stdin.php
  - /.well-known/stripe.txt
  - /webhook*
  - Các nỗ lực path traversal
- User-Agent: curl/8.7.1 (công cụ quét tự động)
- Trạng thái: 567x 404 Not Found, 21x 301 Redirect
- Đột biến: 15:38-15:49 (47-53 req/phút), dừng lại ở 15:50
- Điểm spam: NGHIÊM TRỌNG - Công cụ quét lỗ hổng tự động

Giai đoạn 8 - Phân tích lỗi
✓ Lỗi Nginx: Chỉ có cảnh báo SSL (OCSP stapling) - không có lỗi 502/504
✓ Lỗi Journal: Không có lỗi nghiêm trọng trong khoảng 15:30-16:00
✓ Journal PHP-FPM: Dịch vụ dừng/khởi động lại lúc 15:57-15:58 (sau sự cố)
✓ Log lỗi PHP: Bị từ chối quyền truy cập

Giai đoạn 9 - Phân tích tương quan

DÒNG THỜI GIAN SỰ KIỆN:
15:37     Lưu lượng bình thường (~8 req/phút từ IP quét)
15:38     Bắt đầu tấn công quét - 47 req/phút từ 185.177.72.51
15:38     Bắt đầu đột biến WordPress cron - 53 req/phút từ 46.137.253.133
15:40     NGƯỜI DÙNG BÁO CÁO DOWNTIME BẮT ĐẦU
15:40-49  Tải kết hợp: 100-128 req/phút (2 IP tấn công đồng thời)
15:50     Tấn công DỪNG - lưu lượng giảm xuống 31 req/phút (11 requests từ scanner)
15:50     NGƯỜI DÙNG BÁO CÁO DOWNTIME KẾT THÚC
15:57     Dịch vụ PHP-FPM dừng/khởi động lại (dọn dẹp sau sự cố)

═══ CHUỖI BẰNG CHỨNG ═══

1. [15:37] Lưu lượng cơ bản bình thường
2. [15:38] IP 185.177.72.51 bắt đầu quét lỗ hổng (curl/8.7.1)
3. [15:38] Hoạt động cron WordPress từ IP 46.137.253.133 tăng đột biến
4. [15:40] **BÁO CÁO DOWNTIME** - lưu lượng kết hợp ~120 req/phút
5. [15:40-15:49] Các tiến trình PHP-FPM bão hòa xử lý các request độc hại
6. [15:50] Tấn công dừng - lưu lượng giảm xuống 31 req/phút
7. [15:50] **DỊCH VỤ KHÔI PHỤC**
8. [15:57] Admin thủ công khởi động lại PHP-FPM (dọn dẹp)

═══ CÁC IP ĐÁNG NGỜ ═══

185.177.72.51 - 504 requests - Điểm spam: NGHIÊM TRỌNG
  - Công cụ quét lỗ hổng tự động (curl/8.7.1)
  - Mục tiêu: AWS credentials, phpunit RCE, WordPress login, webhooks
  - 93% lỗi 404 (quét các đường dẫn không tồn tại)
  - Thời lượng tấn công: 12 phút (15:38-15:50)

46.137.253.133 - 577 requests trong khoảng thời gian - Điểm spam: TRUNG BÌNH
  - Các công việc cron nội bộ WordPress (hợp lệ nhưng khối lượng cao)
  - 53% 403 Forbidden (truy cập các đường dẫn bị hạn chế)
  - Có thể được kích hoạt bởi hoạt động của scanner
  - Không độc hại nhưng góp phần gây tải

═══ NGUYÊN NHÂN GỐC RẼ ═══

**Liên quan đến lưu lượng - Tấn công quét lỗ hổng bảo mật**

Sự cố downtime được gây ra bởi đột biến lưu lượng có điều phối hoặc trùng hợp từ hai nguồn:

1. **Nguyên nhân chính**: IP 185.177.72.51 khởi động quét lỗ hổng tự động nhắm vào:
   - Rò rỉ AWS credentials
   - Khai thác PHP Unit RCE
   - Lỗ hổng WordPress
   - Các endpoint webhook
   
2. **Yếu tố đóng góp**: Các công việc cron WordPress từ IP 46.137.253.133 tăng đột biến đồng thời, làm tăng thêm tình trạng cạn kiệt tiến trình PHP-FPM.

3. **Cơ chế tác động**: 
   - Kết hợp 100-120 req/phút làm quá tải pool worker PHP-FPM
   - Hàng đợi PHP-FPM đầy, các request mới bị timeout
   - Website không phản hồi (có thể gặp lỗi 502/504, dù không được ghi log)

4. **Khôi phục**: Tấn công dừng lúc 15:50, lưu lượng bình thường hóa, admin khởi động lại PHP-FPM lúc 15:57 để xóa các tiến trình bị treo.

═══ CÁC RÀO CẢN ═══

Nhiều công cụ không khả dụng, hạn chế độ sâu phân tích:

- **sar (sysstat)**: Chưa được cấu hình - không đọc được /var/log/sysstat/sa23
  - Thiếu: Lịch sử sử dụng CPU, timeline load average, áp lực bộ nhớ
  - Ảnh hưởng: Không thể xác nhận tình trạng bão hòa CPU trong sự cố
  
- **dmesg**: Bị từ chối quyền cho người dùng mcp-reader
  - Thiếu: Các sự kiện OOM killer, kernel panic
  
- **Log lỗi PHP**: Bị từ chối quyền /var/log/php8.0-fpm.log
  - Thiếu: Lỗi nghiêm trọng PHP, timeout trong sự cố

Khắc phục:
```bash
# Cài đặt và bật sysstat
sudo apt install sysstat
sudo systemctl enable sysstat
sudo systemctl start sysstat

# Cấp quyền đọc cho các log hệ thống
sudo setfacl -m u:mcp-reader:r /var/log/php8.0-fpm.log
sudo setfacl -m u:mcp-reader:rx /var/log/sysstat
```

═══ CÁC HÀNH ĐỘNG KHUYẾN NGHỊ ═══

**Ngay lập tức (trong vòng 24 giờ):**
1. Chặn IP 185.177.72.51 ở tầng firewall:
   ```bash
   sudo ufw deny from 185.177.72.51
   ```

2. Cài đặt và cấu hình fail2ban để tự động chặn scanner:
   ```bash
   sudo apt install fail2ban
   # Cấu hình jail để phát hiện quét 404
   ```

3. Xem xét cấu hình pool PHP-FPM:
   ```bash
   # Kiểm tra /etc/php/8.0/fpm/pool.d/corize.co.jp.conf
   # Tăng pm.max_children nếu cần (hiện tại có thể quá thấp)
   # Tải hiện tại cho thấy pool cạn kiệt dưới tấn công
   ```

**Ngắn hạn (trong vòng 1 tuần):**
1. Triển khai giới hạn tốc độ trong Nginx cho corize.co.jp:
   ```nginx
   limit_req_zone $binary_remote_addr zone=corize_limit:10m rate=10r/s;
   limit_req zone=corize_limit burst=20 nodelay;
   ```

2. Cấu hình WordPress cron chạy qua system cron (không qua HTTP):
   ```php
   // wp-config.php
   define('DISABLE_WP_CRON', true);
   ```
   ```bash
   # /etc/cron.d/wordpress-cron
   */5 * * * * corize cd /home/corize/public_html && wp cron event run --due-now
   ```

3. Tăng cường bảo mật WordPress:
   - Cài đặt Wordfence hoặc plugin WAF tương tự
   - Chặn truy cập vào các endpoint nhạy cảm (/.aws/, /vendor/phpunit/, v.v.)
   - Bật chặn XML-RPC

4. Thiết lập cảnh báo giám sát:
   - Cảnh báo khi hàng đợi PHP-FPM > 80% đầy
   - Cảnh báo khi tỷ lệ 404 > 50/phút từ một IP
   - Cảnh báo khi tổng req/phút > 200 cho corize.co.jp

**Dài hạn (trong vòng 1 tháng):**
1. Chuyển sang Cloudflare hoặc dịch vụ CDN/WAF tương tự để bảo vệ chống DDoS

2. Triển khai giám sát đúng cách:
   - Cài đặt và cấu hình sysstat
   - Thiết lập Grafana + Prometheus cho các chỉ số thời gian thực
   - Cấu hình cảnh báo qua email/Slack

3. Xem xét dung lượng máy chủ:
   - Hiện tại: 3.8GB RAM, MySQL tiêu thụ 22%
   - Xem xét: Nâng cấp lên 8GB RAM nếu ngân sách cho phép
   - Thay thế: Tối ưu hóa các truy vấn MySQL, bật query cache

4. Triển khai sao lưu tự động trước sự cố:
   - Cơ sở dữ liệu: Snapshot theo giờ
   - Tệp tin: Sao lưu hàng ngày lên S3/lưu trữ offsite

══════════════════════════════════════
ĐIỀU TRA HOÀN THÀNH: 2026-06-24
Người điều tra: Kiro (Claude Code + MCP Server corize)
Mức độ tin cậy: 92% (Cao)
══════════════════════════════════════
